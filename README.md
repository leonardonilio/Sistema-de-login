# Sistema de Login em PHP

Sistema simples de autenticação desenvolvido em **PHP**, **MySQL**, **HTML**, **CSS** e **JavaScript**, permitindo registro de usuários, login, controle de sessão e logout. Feito para estudos sobre autenticação e integração com banco de dados.

## Descrição
Este projeto permite que usuários se registrem com nome, e-mail e senha, façam login e acessem páginas protegidas. O sistema utiliza PHP procedural, sessões e MySQL para armazenar e validar credenciais. A interface é simples, organizada e pronta para ser expandida conforme a necessidade.

## Estrutura do Projeto
📁 Sistema-de-login  
├── admin_page.php  
├── index.php  
├── login_register.php  
├── logout.php  
├── user_page.php  
├── style.css  
├── script.js  
└── .gitignore  

## Requisitos
- PHP 7+ ou 8+
  
- Servidor local (XAMPP, WAMP, MAMP)
    
- Banco de dados MySQL  

## Como Executar

1. Clone o repositório:
   ```bash
   git clone https://github.com/leonardonilio/Sistema-de-login.git
   ```
   
2. Mova a pasta para o diretório do servidor (ex.: `htdocs` no XAMPP).

3. Crie um banco de dados MySQL (ex.: `login_system`).
 
4. Crie a tabela de usuários necessária no banco.
   
5. Configure o arquivo de conexão com o banco (caso exista).

6. Abra `index.php` no navegador para iniciar o sistema.


## Funcionalidades
- Cadastro de usuários
  
- Login com validação no banco
  
- Página protegida para usuários autenticados
  
- Logout seguro  

## Tecnologias Utilizadas
- PHP
  
- MySQL
  
- HTML5
  
- CSS3
  
- JavaScript  

